*.2-pre1
*.autosave
*.o
dist/

# Meson
/build*/

# Ignore downloaded wrap subprojects (e.g. singleapplication, fetched via .wrap);
# keep the still-vendored lib and the packagefiles overlay tracked.
/subprojects/*/
!/subprojects/packagefiles/
!/subprojects/uglobalhotkey/

# Meson-generated wrap lockfile (scratch)
/subprojects/.wraplock

# ── AI-assistant & IDE artifacts — local only, never tracked ──
# NOTE: docs/ is product documentation here, so ignore ONLY the assistant subtree.
/docs/superpowers/
.superpowers/
.claude/
.claude-mem/
graphify-out/
.idea/
# Unanchored (match at any depth):
CLAUDE.md
AGENTS.md
GEMINI.md
